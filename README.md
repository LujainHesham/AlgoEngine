# AlgoEngine
Multi-Algorithm Decision Engine - An intelligent system that automatically selects and executes the optimal algorithm (DP, Greedy, Divide & Conquer, or Brute Force) based on problem type, input size, time budget, and quality requirements, with experiment mode for algorithm comparison.
